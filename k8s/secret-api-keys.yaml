# Fill in your API keys, then apply:
#   kubectl apply -f k8s/secret-api-keys.yaml
apiVersion: v1
kind: Secret
metadata:
  name: api-keys
  namespace: ai-diplomacy
type: Opaque
stringData:
  OPENROUTER_API_KEY: "REPLACE_ME"
